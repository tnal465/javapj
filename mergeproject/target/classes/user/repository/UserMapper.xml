<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user.repository.UserMapper">

	<select id="findUserById" resultType="user.model.User">
		SELECT id, password, name, nickname, age, gender, address
		FROM users
		WHERE id = #{id}
	</select>
	
    
    <update id="updateUser" parameterType="user.model.User">
        UPDATE users
        SET password = #{password}, nickname = #{nickname}, address = #{address}
        WHERE id = #{id}
    </update>
</mapper>
